# Page · 模型连接

## 职责

- 展示 `models/*.yaml`：API 定义、协议、能力与凭证引用。
- 支持新增模型、测试连接。

## 归属 Agent

- 维护者：用户显式操作；页面只读凭证引用。

## 交互

- 表格展示模型定义与状态。
- 测试连接 → 由 Electron 主进程通过安全存储取凭证，调用远端 API。

## 约束

- 密钥绝不写入 YAML、页面、trace 或 Git（PRD F2 硬约束）。
- 仅展示 `Keychain / <REF>` 形式的引用。
