# Page · Skills

## 职责

- 列出 `agents/*/skills/*.md`：Skill 名称、用途、输入输出、宿主 Agent。
- 支持新建 Skill、校验定义。

## 归属 Agent

- 宿主：`agents/first-mate/agent.md`、`agents/interface-worker/agent.md` 等。

## 交互

- 卡片展示 Skill 的输入、输出与文件路径。
- 校验 → 校验 Markdown 结构与必填字段。

## 约束

- Skill 只读源码库；修改走编辑 + Git 提交。
