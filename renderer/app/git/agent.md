# Page · Git 变更

## 职责

- 展示工作区未提交变更：文件、增删行数、关联 trace。
- 支持创建本地提交、查看 diff、提交历史。

## 归属 Agent

- 提交者：用户显式确认后执行 `git commit`；系统不自动提交。

## 交互

- 勾选文件 → 展示 diff → 填写提交信息 → 确认提交。
- 每次 Agent 写入的文件均关联 trace 以便反查。

## 约束

- 只在当前工作区内执行 Git；禁止推送与强制操作。
- 密钥、凭证文件不进入提交。
