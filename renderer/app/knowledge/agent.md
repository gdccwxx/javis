# Page · 知识工作区

## 职责

- 展示 Git 管理的 `knowledge/` 文件树与文档正文。
- 所有变更可追溯，可直接查看 diff 并提交。

## 归属 Agent

- 只读：任意船员可读取；编辑由用户确认后执行。

## 交互

- 文件树筛选、选中文件 → 渲染 Markdown 正文。
- 编辑文件 → 写入工作区，交由 Git 记录。

## 约束

- 知识文件是唯一真源；不允许页面内缓存污染。
- 删除、覆盖需先展示 diff，用户确认后执行。
